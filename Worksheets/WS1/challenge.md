---
title: "Challenge: Worksheet 1"
type: challenge
---

# Challenge: Worksheet 1

Time to combine what you've learned about data types, variables, boolean logic, conditionals, and strings.

---

## Requirements

Complete **at least 2 out of 3** of the problems below. Put your finished work in its own file (e.g. `ws1_challenge.py`) and let the President or VP know which problems you tackled before submitting.

---

## Problem 1: Age Category

Ask the user for their age, store it in a variable, and print which group they belong to:

| Age range | Group     |
|-----------|-----------|
| under 13  | Child     |
| 13–17     | Teenager  |
| 18–64     | Adult     |
| 65+       | Senior    |

```python
age = int(input("How old are you? "))
# your if/elif/else here
```

---

## Problem 2: Letter Hunt

Given a string, check whether it contains the letter `'a'` and print either:

- `"Found 'a'"`
- `"No 'a' found"`

```python
word = input("Enter a word: ")
# your check here
```

> Hint: the `in` operator works on strings — `"a" in word` is `True` or `False`.

---

## Problem 3: Both Positive

Given two variables `a` and `b`, print `"Both are positive"` **only** when **both** numbers are positive. Otherwise, print nothing (or a different message of your choice).

```python
a = int(input("a: "))
b = int(input("b: "))
# your boolean logic here
```

---

## Reflection

Once you've finished, jot down answers to:

- Which section did you find the most challenging?
- What's something new you learned about strings?
